version: "3.4"

services:

    openssh:
        image: harshavardhanj/openssh:without-bash
        ports:
            - "2222:22"
        environment:
            - USER=example
            - PASSWORD=example
